#include<stdio.h>
main()
{
	int n,i,a[n],max,min;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	max=a[0];
	for(i=1;i<n;i++)
	{
		
		if(a[i]>max)
		max=a[i];
	}
	printf("Max%d\n",max);
	min=a[0];
	for(i=1;i<n;i++)
	{
		if(a[i]<min)
		min=a[i];
	}
	printf("Min=%d\n",min);
}
